#include "main.h"
/**
 * rev_string - reverses a string.
 * @s: string to reverse.
 *
 * Return: void
 */
void rev_string(char *s)
{
	int len = 0;

	while (*(s + len) != '\0')
	{
		if (*(s + len + 1) == '\0')
		{

		}
	}
}
