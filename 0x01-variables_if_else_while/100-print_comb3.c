#include <stdio.h>
/**
 * main - prints all possible different combinations of two digits.
 *
 * Return: 0
 */
int main(void)
{
	int i = 47, j = 48;

	while (i < 58)
	{
		while (j < 58)
		{
			if (i == j)
				continue;
			putchar(i);
			putchar(j);
			if (i == )
			{
				putchar(44);
				putchar(32);
			}
		}
	}
}
