#include <stdio.h>
/**
 * main -  prints all possible combinations of two two-digit numbers.
 *
 * Return: returns 0
 */
int main(void)
{
	int i = 48, j = 48, k = 48, l = 48;

	while (i < 58)
	{
		while (j < 58)
		{
			while (j < 58)
			{
				while (l < 58)
				{

				}
			}
		}
	}
	putchar('\n');
	return (0);
}
