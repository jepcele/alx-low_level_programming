#include main.h
/**
 * leet -  function that encodes a string into 1337.
 * @s: string to encode
 *
 * Return: char
 */
char *leet(char *s)
{
	int i = 0;
	char arr[5][2] = {{'a', 'A'}, {'e', 'E'}, {'o', 'O'}, {'t', 'T'}, {'l', 'L'}};
	char code[5] = {'4', '3', '0', '7', '1'};

	while(*(s + i) != '\0')
	{
		
	}
}
